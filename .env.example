# Authentication
# Bcrypt hash of the universal admin password (generate with `node -e "require('bcrypt').hash('MyTempPassword', 12).then(console.log)"`)
AUTH_ADMIN_BCRYPT_HASH="$2b$12$cI3T7MricZ/3pW4tRmFnheop.DSWt9hvRKsUVjO8xoSddf9YYL33K"
# 32-byte random secret for 1h access tokens (e.g. `openssl rand -base64 32`)
AUTH_JWT_SECRET_ACCESS="kP5Q52N0/1nXMo7a7j8jvZNPmlQG2zSg2KZP05TvR7E="
# Separate 32-byte secret for 7d refresh tokens (rotate independently)
AUTH_JWT_SECRET_REFRESH="aHVodHlwZVNlY3JldFNlZWROZWVkQmVybmFyeVN0cmluZw=="
# Optional: override default cost (defaults to 12). Must be integer between 8 and 14.
AUTH_BCRYPT_COST="12"

# Existing relevant settings
# DOWNLOAD_SECRET=dev-download-secret-change-me
# ALLOWED_ORIGINS=http://localhost:5173
# REQUIRE_SIGNED_DOWNLOADS=true
# SSE_MAX_CONN_PER_IP=2
